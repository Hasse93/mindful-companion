# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Env / secrets — never commit real keys
.env
backend/.env
*.local

# Local SQLite dev DB
*.db

# ML artifacts. Commit the small quantized ONNX models (≈64 MB) so the app
# deploys from the repo; ignore the large FP32 training output + experiment logs.
backend/models/emotion/
mlruns/
*.local

# Node / Next.js
node_modules/
.next/
out/
npm-debug.log*

# OS / editor
.DS_Store
.idea/
.vscode/
